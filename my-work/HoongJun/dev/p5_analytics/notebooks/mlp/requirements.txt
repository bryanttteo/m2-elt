# MLP pipeline dependencies. Install with:  pip install -r requirements.txt
# (run.sh does NOT install — it only executes the pipeline.)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0
joblib>=1.3
matplotlib>=3.7
pyarrow>=12.0                      # parquet cache for the order-grain table

# --- BigQuery ingestion ---
google-cloud-bigquery>=3.0
google-cloud-bigquery-storage>=2.0   # faster .to_dataframe() pulls
db-dtypes>=1.0                        # BQ DATE/TIMESTAMP -> pandas dtypes

# --- Optional: only needed if model.name=xgboost ---
xgboost>=2.0
